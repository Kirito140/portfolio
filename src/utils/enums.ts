export enum ToastType {
  SUCCESS = "success",
  INFO = "info",
  WARN = "warning",
  ERROR = "danger",
  SECONDARY = "secondary",
  CONTRAST = "contrast",
}

export enum TimelineIcons {
  WORK = "work",
  EDUCATION = "education",
  OTHER = "other",
}